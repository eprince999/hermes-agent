# Raspberry Pi / 灯上录制专用。不要在灯上安装 requirements-train.txt
# （那份会拉 400MB+ 的 PyTorch，SD 卡会爆。）
# picamera2 不要 pip 装：用系统包 + 让 venv 看见它。见 enable_pi_camera.sh
# 舵机不要 pip install lerobot：用 `sudo uv run` 或 runtime 自带的 scservo_sdk。
pillow>=10,<12
